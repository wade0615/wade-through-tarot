# Wade Through Tarot - 塔羅牌占卜應用

一個現代化的塔羅牌占卜 Web 應用，提供直觀的塔羅牌閱讀體驗和完整的牌面圖鑑。

**讓直覺引導你找到答案** ✨

---

## 📖 目錄

- [產品導向](#-產品導向)
  - [專案簡介](#-專案簡介)
  - [主要功能](#-主要功能)
  - [使用說明](#-使用說明)
  - [設計特色](#-設計特色)
- [技術導向](#-技術導向)
  - [技術架構](#-技術架構)
  - [專案結構](#-專案結構)
  - [快速開始](#-快速開始)
  - [開發指南](#-開發指南)
  - [貢獻指南](#-貢獻指南)

---

# 🎯 產品導向

> 適合產品經理、設計師、一般用戶閱讀

## 📝 專案簡介

Wade Through Tarot 是一個為現代用戶打造的線上塔羅牌占卜平台，結合直觀的用戶體驗與深度的塔羅牌知識，讓每個人都能輕鬆獲得心靈指引。

### 🎯 專案願景

- **易用性優先**：即使是初次接觸塔羅牌的用戶也能快速上手
- **知識豐富**：提供完整的 78 張塔羅牌詳細資訊
- **體驗優化**：流暢的動畫效果與現代化的視覺設計
- **跨平台支持**：完美適配手機、平板、桌面各種設備

## ✨ 主要功能

### 🔮 占卜系統

#### 三種牌陣選擇

1. **單張牌占卜**
   - 適合：快速指引、每日運勢
   - 特色：簡單快速，適合初學者

2. **三張牌占卜**
   - 牌位：過去 → 現在 → 未來
   - 特色：了解事件發展脈絡
   - 適合：關係、決策、事業發展

3. **凱爾特十字牌陣**
   - 牌位：10 個位置，涵蓋完整情境
   - 特色：最全面的分析結果
   - 適合：重大決策、深度自我探索

#### 占卜流程

```
選擇牌陣 → 輸入問題（可選）→ 專注冥想 → 選擇牌卡 → 查看結果
```

#### 結果呈現

- **正逆位顯示**：清楚標示每張牌的方向
- **含義解讀**：詳細的正位與逆位含義
- **關鍵詞提煉**：快速掌握核心訊息
- **整體建議**：針對問題給予具體指引

### 📚 塔羅牌圖鑑

#### 完整收錄

- **大阿爾克納（Major Arcana）**：22 張核心牌
  - 從愚者到世界，代表人生旅程的重大課題

- **小阿爾克納（Minor Arcana）**：56 張
  - **聖杯（Cups）**：情感、關係、創造力
  - **金幣（Pentacles）**：物質、財富、工作
  - **寶劍（Swords）**：思想、溝通、挑戰
  - **權杖（Wands）**：行動、熱情、靈感

#### 瀏覽體驗

- **分類導航**：按牌組快速篩選
- **卡片展示**：精美的牌面圖像
- **詳細資訊**：點擊查看完整描述、象徵意義、數字學、占星學關聯
- **響應式網格**：自動適應不同螢幕尺寸

### 🔗 進階功能

#### ChatGPT 整合

- **一鍵分享**：將占卜結果直接傳送到 ChatGPT
- **深度分析**：利用 AI 進行更個人化的解讀
- **對話延伸**：持續探討與問題相關的面向

#### 分享與保存

- **複製結果**：一鍵複製完整占卜內容
- **多平台分享**：輕鬆分享給朋友或儲存到筆記

## 📱 使用說明

### 🌟 開始你的第一次占卜

#### 步驟 1：選擇牌陣

進入首頁後，你會看到三種牌陣選項：
- 新手推薦：從「單張牌」開始
- 想了解趨勢：選擇「三張牌」
- 需要全面分析：選擇「凱爾特十字」

#### 步驟 2：提出問題（可選）

- 在問題欄位輸入你想詢問的問題
- 建議：問題越具體，解讀越精確
- 範例問題：
  - ✅ 「我該如何改善與同事的關係？」
  - ❌ 「我的未來？」（太籠統）

#### 步驟 3：選擇牌卡

- 點擊「開始占卜」進入選牌畫面
- 深呼吸，專注於你的問題
- 憑直覺點擊吸引你的牌卡
- 依序選完所需數量的牌

#### 步驟 4：查看結果

- 系統會自動解析每張牌的含義
- 閱讀整體建議，思考如何應用到生活中
- 可以選擇複製結果或傳送到 ChatGPT

### 🗂️ 探索塔羅牌圖鑑

#### 瀏覽方式

1. 點擊首頁的「查看所有牌面」按鈕
2. 使用頂部的分類標籤切換牌組
3. 滾動瀏覽，欣賞每張牌的設計

#### 學習牌義

1. 點擊任意一張牌
2. 彈出視窗會顯示：
   - **基本資訊**：牌名、編號、所屬牌組
   - **視覺描述**：牌面圖像的詳細說明
   - **核心含義**：正位與逆位的關鍵意義
   - **關鍵詞**：快速記憶要點
   - **深度分析**（進階）：
     - 象徵主義
     - 數字學意義
     - 占星學關聯
     - 神話背景
     - 實用建議

#### 學習技巧

- **每日一牌**：每天抽一張牌，記錄感受
- **對比學習**：比較相似牌面的差異
- **情境應用**：想像不同情境下的牌義

## 🎨 設計特色

### 視覺設計

- **神秘氛圍**
  - 深紫色與藍色漸變背景
  - 星空點綴，營造靈性氛圍
  - 柔和的色調，不刺眼舒適

- **毛玻璃美學**
  - 半透明卡片設計
  - 現代感與神秘感的完美結合
  - 層次分明，視覺焦點清晰

### 互動體驗

- **流暢動畫**
  - 翻牌動畫：模擬真實翻牌體驗
  - 懸停效果：即時視覺回饋
  - 過渡動畫：頁面切換自然流暢

- **觸感回饋**
  - 按鈕點擊有明確反饋
  - 卡片選擇有視覺提示
  - 操作符合直覺

### 響應式設計

- **多設備適配**
  - 📱 手機：單欄排列，大按鈕設計
  - 📱 平板：雙欄布局，平衡美觀與資訊
  - 💻 桌面：多欄展示，充分利用空間

- **PWA 支持**
  - 可安裝到主畫面
  - 離線基礎功能
  - 快速啟動體驗

---

# 💻 技術導向

> 適合開發者、技術人員閱讀

## 🛠️ 技術架構

### 核心技術棧

#### 前端框架

- **Next.js 15**
  - App Router 架構
  - 伺服器端渲染（SSR）
  - 靜態站點生成（SSG）
  - API Routes 支持
  - 自動代碼分割

- **React 18**
  - 函數式組件
  - Hooks API
  - Concurrent Features
  - Suspense 支持

#### 開發語言

- **TypeScript**
  - 完整類型覆蓋
  - 介面定義清晰
  - 編譯時錯誤檢查
  - 更好的 IDE 支持

#### 樣式方案

- **Tailwind CSS**
  - Utility-first CSS
  - 響應式設計
  - 自定義主題
  - 深色模式支持

#### 狀態管理

- **Zustand**
  - 輕量級（~1KB）
  - 簡單的 API
  - 無需 Provider
  - TypeScript 友好
  - 持久化支持

#### 開發工具

- **ESLint** - 代碼質量檢查
- **Prettier** - 代碼格式化（建議配置）
- **PostCSS** - CSS 後處理
- **TypeScript ESLint** - TS 特定規則

### 技術特點

#### 性能優化

- **圖片優化**
  - Next.js Image 組件
  - 自動 WebP 轉換
  - 延遲載入（Lazy Loading）
  - 響應式圖片

- **程式碼優化**
  - Tree Shaking
  - 自動代碼分割
  - 動態導入
  - 生產環境壓縮

#### SEO 優化

- 伺服器端渲染
- Meta 標籤管理
- Sitemap 生成（建議實作）
- Schema.org 結構化資料（建議實作）

## 📁 專案結構

```
wade-through-tarot/
├── client/                          # Next.js 前端應用
│   ├── public/                      # 靜態資源目錄
│   │   ├── cards/                   # 塔羅牌圖片資源
│   │   │   ├── fool.webp
│   │   │   ├── magician.webp
│   │   │   └── ...                  # 其他 78 張牌圖片
│   │   ├── favicon.ico              # 網站圖標
│   │   └── manifest.json            # PWA 配置（建議添加）
│   │
│   ├── src/                         # 源代碼目錄
│   │   ├── app/                     # Next.js App Router
│   │   │   ├── cards/               # 牌面圖鑑頁面
│   │   │   │   └── page.tsx         # /cards 路由
│   │   │   ├── globals.css          # 全局樣式
│   │   │   ├── layout.tsx           # 根布局組件
│   │   │   └── page.tsx             # 首頁 / 路由
│   │   │
│   │   ├── components/              # React 組件
│   │   │   ├── CardDeck.tsx         # 牌組展示（開扇布局）
│   │   │   ├── CardModal.tsx        # 牌面詳情彈窗
│   │   │   ├── ReadingResult.tsx    # 占卜結果顯示
│   │   │   ├── SelectionView.tsx    # 選牌界面
│   │   │   ├── SetupView.tsx        # 占卜設置界面
│   │   │   ├── SpreadLayout.tsx     # 牌陣布局組件
│   │   │   └── TarotCard.tsx        # 單張塔羅牌組件
│   │   │
│   │   ├── data/                    # 資料檔案
│   │   │   └── tarotCards.ts        # 78 張塔羅牌完整資料
│   │   │                            # （含義、描述、關鍵詞等）
│   │   │
│   │   ├── store/                   # 狀態管理
│   │   │   └── tarotStore.ts        # Zustand Store
│   │   │                            # （牌陣、選牌、結果狀態）
│   │   │
│   │   └── utils/                   # 工具函數
│   │       └── helpers.ts           # 通用輔助函數
│   │                                # （洗牌、格式化等）
│   │
│   ├── .env.local                   # 環境變數（需自行創建）
│   ├── .env.example                 # 環境變數範例
│   ├── .eslintrc.json              # ESLint 配置
│   ├── .gitignore                  # Git 忽略配置
│   ├── next.config.js              # Next.js 配置
│   ├── package.json                # 依賴配置
│   ├── postcss.config.js           # PostCSS 配置
│   ├── tailwind.config.ts          # Tailwind CSS 配置
│   └── tsconfig.json               # TypeScript 配置
│
├── .gitignore                       # 專案根目錄 Git 忽略
└── README.md                        # 專案說明文件
```

### 關鍵檔案說明

#### 配置檔案

- **`next.config.js`**：Next.js 配置，可設定圖片域名、環境變數等
- **`tailwind.config.ts`**：Tailwind 主題自定義（顏色、字體、斷點）
- **`tsconfig.json`**：TypeScript 編譯選項
- **`.eslintrc.json`**：代碼風格與質量規則

#### 核心檔案

- **`src/data/tarotCards.ts`**：所有塔羅牌資料的單一來源
- **`src/store/tarotStore.ts`**：全局狀態管理邏輯
- **`src/app/layout.tsx`**：全站共用布局與 metadata

## 🚀 快速開始

### 環境要求

- **Node.js** >= 18.0.0
- **npm** >= 9.0.0 或 **yarn** >= 1.22.0
- 現代瀏覽器（Chrome, Firefox, Safari, Edge）

### 安裝步驟

#### 1. 克隆專案

```bash
git clone https://github.com/yourusername/wade-through-tarot.git
cd wade-through-tarot
```

#### 2. 安裝依賴

```bash
cd client
npm install

# 或使用 yarn
yarn install
```

#### 3. 環境變數設置

```bash
# 複製環境變數範例檔案
cp .env.example .env.local
```

編輯 `.env.local`，填入以下變數：

```env
# Google Analytics（選填）
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# Google AdSense（必填）
NEXT_PUBLIC_ADSENSE_ID=ca-pub-XXXXXXXXXXXXXXXX

# 網站 URL（必填）
NEXT_PUBLIC_SITE_URL=https://yourdomain.com

# 開發模式（選填，本地開發建議設為 true）
NEXT_PUBLIC_DEV_MODE=true
```

**環境變數說明**：

| 變數名稱 | 必填 | 說明 | 取得方式 |
|---------|------|------|---------|
| `NEXT_PUBLIC_GA_MEASUREMENT_ID` | 否 | Google Analytics 追蹤 ID | [Google Analytics](https://analytics.google.com/) |
| `NEXT_PUBLIC_ADSENSE_ID` | 是 | Google AdSense Publisher ID | [Google AdSense](https://www.google.com/adsense/) |
| `NEXT_PUBLIC_SITE_URL` | 是 | 網站完整 URL | 部署後的域名 |
| `NEXT_PUBLIC_DEV_MODE` | 否 | 開發模式開關（關閉廣告） | `true` 或 `false` |

#### 4. 啟動開發伺服器

```bash
npm run dev

# 或使用 yarn
yarn dev
```

開啟瀏覽器訪問 `http://localhost:3000`

#### 5. 構建生產版本

```bash
# 構建
npm run build

# 啟動生產伺服器
npm start

# 或一次性構建並啟動
npm run build && npm start
```

### 常用指令

```bash
# 開發模式
npm run dev

# 生產構建
npm run build

# 啟動生產伺服器
npm start

# 程式碼檢查
npm run lint

# 類型檢查
npx tsc --noEmit
```

## 🔧 開發指南

### 資料結構

#### TarotCard 介面

```typescript
interface TarotCard {
  id: string;                      // 唯一識別碼
  name: string;                    // 中文牌名
  nameEn: string;                  // 英文牌名
  suit: "major" | "cups" | "pentacles" | "swords" | "wands";
  number?: number;                 // 牌號（1-14，Page=11, Knight=12...）
  meaning: {
    upright: string[];             // 正位含義
    reversed: string[];            // 逆位含義
  };
  description: string;             // 牌面詳細描述
  keywords: string[];              // 關鍵詞
  imageUrl: string;                // 圖片路徑
  deepAnalysis?: {                 // 深度分析（選填）
    symbolism: string;             // 象徵意義
    numerology: string;            // 數字學
    astrology: string;             // 占星學
    mythology: string;             // 神話背景
    practicalAdvice: string;       // 實用建議
    loveReading: string;           // 愛情解讀
    careerReading: string;         // 事業解讀
    healthReading: string;         // 健康解讀
  };
}
```

#### Store 狀態結構

```typescript
interface TarotStore {
  // 狀態
  spreadType: 'single' | 'three' | 'celtic';
  question: string;
  selectedCards: DrawnCard[];
  isReading: boolean;

  // 方法
  setSpreadType: (type: string) => void;
  setQuestion: (question: string) => void;
  addCard: (card: DrawnCard) => void;
  startReading: () => void;
  reset: () => void;
}
```

### 添加新功能

#### 1. 新增牌陣類型

**步驟**：

1. 在 `src/store/tarotStore.ts` 更新 `SpreadType` 類型
2. 在 `src/components/SetupView.tsx` 添加選項
3. 在 `src/components/SpreadLayout.tsx` 實作布局
4. 更新 `src/components/ReadingResult.tsx` 的解讀邏輯

**範例**：添加「關係牌陣」

```typescript
// 1. 更新類型
type SpreadType = 'single' | 'three' | 'celtic' | 'relationship';

// 2. 添加配置
const SPREAD_CONFIGS = {
  relationship: {
    name: '關係牌陣',
    count: 7,
    positions: ['自己', '對方', '過去', '現在', '未來', '挑戰', '結果']
  }
};

// 3. 實作布局組件
function RelationshipLayout({ cards }: { cards: DrawnCard[] }) {
  // 布局邏輯
}
```

#### 2. 擴展牌面資料

編輯 `src/data/tarotCards.ts`：

```typescript
{
  id: "new-card",
  name: "新牌名",
  nameEn: "New Card",
  suit: "major",
  number: 22,
  meaning: {
    upright: ["含義1", "含義2"],
    reversed: ["逆位含義1", "逆位含義2"]
  },
  description: "詳細描述...",
  keywords: ["關鍵詞1", "關鍵詞2"],
  imageUrl: "/cards/new-card.webp",
  deepAnalysis: {
    // 深度分析內容
  }
}
```

#### 3. 自定義主題

編輯 `tailwind.config.ts`：

```typescript
module.exports = {
  theme: {
    extend: {
      colors: {
        primary: {
          50: '#f0f9ff',
          // ... 其他色階
          900: '#0c4a6e',
        }
      },
      fontFamily: {
        sans: ['Noto Sans TC', 'sans-serif'],
      }
    }
  }
}
```

### 效能優化建議

#### 圖片優化

```tsx
// 使用 Next.js Image 組件
import Image from 'next/image';

<Image
  src="/cards/fool.webp"
  alt="愚者"
  width={300}
  height={500}
  priority // 重要圖片優先載入
  placeholder="blur" // 模糊預覽
/>
```

#### 動態導入

```tsx
// 延遲載入重量級組件
import dynamic from 'next/dynamic';

const HeavyComponent = dynamic(
  () => import('./HeavyComponent'),
  { loading: () => <p>載入中...</p> }
);
```

#### Memo 化組件

```tsx
import { memo } from 'react';

export const TarotCard = memo(({ card }: Props) => {
  // 組件邏輯
});
```

### 測試（建議實作）

```bash
# 安裝測試依賴
npm install -D @testing-library/react @testing-library/jest-dom jest

# 執行測試
npm test
```

範例測試：

```typescript
import { render, screen } from '@testing-library/react';
import { TarotCard } from '@/components/TarotCard';

test('renders tarot card correctly', () => {
  const mockCard = { /* ... */ };
  render(<TarotCard card={mockCard} />);
  expect(screen.getByText('愚者')).toBeInTheDocument();
});
```

### 部署

#### Vercel（推薦）

```bash
# 安裝 Vercel CLI
npm i -g vercel

# 部署
vercel

# 生產部署
vercel --prod
```

#### 其他平台

- **Netlify**：連接 Git 倉庫自動部署
- **AWS Amplify**：支持自動 CI/CD
- **自架 VPS**：使用 PM2 管理 Node.js 進程

## 🤝 貢獻指南

我們歡迎所有形式的貢獻！

### 如何貢獻

1. **Fork 專案**
2. **創建功能分支** (`git checkout -b feature/AmazingFeature`)
3. **提交變更** (`git commit -m 'Add some AmazingFeature'`)
4. **推送到分支** (`git push origin feature/AmazingFeature`)
5. **開啟 Pull Request**

### 程式碼規範

- 遵循 ESLint 規則
- 使用 TypeScript 嚴格模式
- 組件採用函數式風格
- 適當添加註釋

### 提交訊息規範

```
<type>(<scope>): <subject>

<body>

<footer>
```

**Type**:
- `feat`: 新功能
- `fix`: 修復 bug
- `docs`: 文件變更
- `style`: 程式碼格式（不影響功能）
- `refactor`: 重構
- `test`: 測試相關
- `chore`: 建構過程或輔助工具變更

**範例**:
```
feat(cards): 新增塔羅牌收藏功能

- 實作收藏按鈕組件
- 添加本地儲存邏輯
- 更新卡片詳情頁面

Closes #123
```

### 回報問題

使用 GitHub Issues 回報問題時，請包含：

- **環境資訊**：瀏覽器版本、作業系統
- **重現步驟**：如何觸發問題
- **預期行為**：應該發生什麼
- **實際行為**：實際發生什麼
- **截圖**：如果可能，提供截圖

## 📄 授權

本專案採用 MIT 授權條款。詳見 [LICENSE](LICENSE) 檔案。

## 🙏 致謝

- **塔羅牌圖片**：感謝圖片創作者提供精美的牌面設計
- **開源社區**：感謝 Next.js、React、Tailwind CSS 團隊
- **貢獻者**：感謝所有參與專案的開發者
- **使用者**：感謝每一位使用者的回饋與支持

---

## 📞 聯絡方式

- **網站**：[https://your-domain.com](https://your-domain.com)
- **Email**：your-email@example.com
- **GitHub Issues**：[提出問題](https://github.com/yourusername/wade-through-tarot/issues)

---

**開發愉快！願塔羅牌的智慧照亮你的開發旅程** ✨🔮
